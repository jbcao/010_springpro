<?xml version="1.0" encoding="UTF-8" ?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="k_sm.mapper.UserMapper">
    <insert id="insert" parameterType="usersm">
        insert into usersm values(#{userId},#{uName},#{uPass})
    </insert>
</mapper>